import styled from "styled-components";

export const Error = styled.p`
   width: 90%;
   text-align: center;
   padding: 40px 0;
   font-size: 18px;
   color: red;
`;

export const Msg = styled(Error)`
   color: blue;
`;